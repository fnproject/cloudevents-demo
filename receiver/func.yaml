version: 0.0.43
runtime: go
entrypoint: ./func
type: async
format: cloudevent
timeout: 360
config:
  DETECT_SENSITIVITY: "0.3"
  FN_APP_NAME: cloudevents
  IS_DOCKER4MAC_LOCAL: "true"
expects:
  config:
  - name: FN_API_URL
    required: true
